# zammad_time_export
Python script to export times from zammad

# install
pip install -r requirements.txt

# usage
python3 script.py --url https://help.website.com/api/v1/ --username username@domain.com --password password --year 2022 --month 10
